import { createBrowserHistory, createMemoryHistory, createHashHistory, } from 'history';
import { createHistory$ } from './createHistory$';
function isHistory(x) {
    return !!x.createHref;
}
export function makeHistoryDriver(options) {
    var history;
    if (options && isHistory(options)) {
        history = options;
    }
    else {
        history = createBrowserHistory(options);
    }
    return function historyDriver(sink$) {
        return createHistory$(history, sink$);
    };
}
export function makeServerHistoryDriver(options) {
    var history = createMemoryHistory(options);
    return function serverHistoryDriver(sink$) {
        return createHistory$(history, sink$);
    };
}
export function makeHashHistoryDriver(options) {
    var history = createHashHistory(options);
    return function hashHistoryDriver(sink$) {
        return createHistory$(history, sink$);
    };
}
//# sourceMappingURL=drivers.js.map